{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-read server.ts",
    "start": "deno run --allow-net --allow-read server.ts",
    "build": "rm -rf dist && mkdir -p dist && cp public/index.html public/styles.css public/app.js dist/"
  },
  "compilerOptions": {
    "lib": ["deno.window", "dom"]
  }
}
